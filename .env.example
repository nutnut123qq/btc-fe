# Server-only destination used by Next rewrites. Docker should override this.
BACKEND_INTERNAL_URL=http://127.0.0.1:5197

# Set only for standalone/Docker builds. Local build/start leaves this unset.
NEXT_STANDALONE=0
